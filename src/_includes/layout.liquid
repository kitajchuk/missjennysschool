<!doctype html>
<html lang="en">
  <head>
    {% render "metadata", site: site %}
    <title>{% if title %}{{ title }} | {% endif %}{{ site.title }}</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <header>
      <nav></nav>
    </header>
    <main>
      {{ content }}
    </main>
    <footer></footer>
    <script type="module" src="/js/index.js"></script>
  </body>
</html>