<!doctype html>
<html lang="en">
  <head>
    <title>{% if title %}{{ title }} | {% endif %}Miss Jenny's School</title>
    <link href="/css/styles.css" rel="stylesheet" />
  </head>
  <body class="{{ class }}">
    <nav></nav>
    <main>
      {{ content }}
    </main>
    <footer></footer>
  </body>
</html>